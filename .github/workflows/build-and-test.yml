name: Build & Test

on:
  push:
    branches: ["*"]
  pull_request:

permissions:
  contents: read

jobs:
  Tests:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            rustup_toolchain: stable
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-musl
            rustup_toolchain: stable
          - os: ubuntu-22.04
            target: aarch64-unknown-linux-gnu
            rustup_toolchain: stable
          - os: macos-14
            target: aarch64-apple-darwin
            rustup_toolchain: stable
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ matrix.rustup_toolchain }}

      - name: Cargo build (Native TLS)
        run: |
          cargo build --all --no-default-features
          cargo clean

      - name: Cargo build (Rust TLS)
        run: cargo build --all

      - name: Set up mock
        run: |
          env | grep PATH
          cd ~/bin
          curl https://github.com/dhuan/mock/releases/download/v1.4.16/mock_v1.4.16_linux-386.tar.gz | tar xz

      - name: E2E test
        run: make test
